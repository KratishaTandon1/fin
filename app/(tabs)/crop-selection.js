import {
    StyleSheet
} from 'react-native';

function CropSelectionScreen() {
  // ... (use the complete crop advisory code I provided in the previous message)
}

const styles = StyleSheet.create({
  // ... (use the complete styles I provided in the previous message)
});

export default CropSelectionScreen;
